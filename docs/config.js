// Public settings only. Never put passwords, SMTP credentials or GitHub tokens here.
window.PORTFOLIO_CONFIG = {
  // Example: "https://portfolio-api.example.com" (no trailing slash).
  // null = standalone GitHub Pages template. Localhost auto-connects to the local API.
  apiBaseUrl: null
};
