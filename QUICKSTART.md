# เริ่มใช้งาน MyPublic

เว็บไซต์จริง: https://nyequirel.github.io/myportal/ · Repository: https://github.com/nyequirel/myportal

สำหรับ repository นี้ตั้งค่า Pages เป็น **Deploy from a branch → main → /docs** แล้ว อัปเดตข้อมูลสาธารณะใน `docs` และ push ไป `main` เพื่อเผยแพร่รุ่นใหม่

ทำต่อในเครื่องได้โดยไม่ต้องเข้าสู่ระบบ GitHub

## ดูหน้าเว็บ

ดับเบิลคลิก **`start-local.cmd`** แล้วเปิด **http://localhost:3000** ถ้าเซิร์ฟเวอร์เปิดอยู่แล้ว ตัวช่วยจะแจ้ง URL โดยไม่เปิดเซิร์ฟเวอร์ซ้ำ

หากย้ายไปเครื่องใหม่ ให้ติดตั้ง Node.js 22.13 ขึ้นไป และรัน `npm.cmd install` ในโฟลเดอร์โปรเจกต์ก่อน เก็บหน้าต่างเซิร์ฟเวอร์ไว้ขณะใช้งาน ปิดด้วย Ctrl+C

## ทดลองเข้าสู่ระบบโดยไม่ต้องใช้โทรศัพท์

1. คัดลอก `server/.env.example` เป็น `server/.env`
2. กำหนด `OWNER_EMAIL` และ `OWNER_PASSWORD` (อย่างน้อย 12 ตัวอักษร) ในไฟล์นี้บนเครื่องของคุณ
3. รัน `npm.cmd run owner:create` แล้วลบค่า `OWNER_PASSWORD` ออกจากไฟล์
4. เปิดเซิร์ฟเวอร์และเข้า http://localhost:3000/login.html
5. เมื่อกรอกรหัสผ่านถูกต้อง ให้เปิดไฟล์ **`server/data/outbox/latest-email.txt`** เพื่ออ่าน OTP ที่ใช้ทดสอบ

ขั้นตอนนี้ใช้ `MAIL_MODE=outbox` จึงยังไม่ส่งอีเมลจริง และยังตรวจทั้งรหัสผ่านและ OTP ตามปกติ การตั้งค่า SMTP สำหรับส่งอีเมลจริงอยู่ใน README

## แก้โปรไฟล์และผลงาน

หลังเข้าสู่ระบบ แก้ข้อมูลในหลังบ้านแล้วกด **บันทึกการเปลี่ยนแปลง** ผลงานที่ปิด “เผยแพร่” จะเป็นฉบับร่างใน backend

ถ้าใช้เฉพาะเว็บ static บน GitHub Pages ให้แก้ `docs/data/portfolio.json` หรือใช้ปุ่ม **ส่งออกไฟล์สำหรับ GitHub** ในหลังบ้าน แล้วนำไฟล์ไปแทน JSON ดังกล่าว

เมื่อถอนเผยแพร่ผลงานที่เคยอยู่ในไฟล์ static ให้ส่งออกและอัปโหลดไฟล์ฉบับใหม่ด้วย เพราะข้อมูลสำรองบน Pages จะเปลี่ยนเมื่อ push เท่านั้น

## นำเสนอผลงานแยกเป็นรายโปรเจกต์

คลิกการ์ดผลงานเพื่อเปิดหน้ารายละเอียดของโปรเจกต์นั้น เช่น:

```text
http://localhost:3000/project.html?id=sample-web
https://USERNAME.github.io/mypublic/project.html?id=sample-web
```

หน้าแต่ละโปรเจกต์มีปุ่ม **คัดลอกลิงก์ผลงาน** สามารถส่งลิงก์นี้ให้ผู้อื่นเปิดดูโดยตรงได้ โดยไม่ต้องเข้าสู่ระบบ

ในหลังบ้าน ให้เปิด **รายละเอียดสำหรับนำเสนอโปรเจกต์** ของผลงานนั้น แล้วกรอกภาพรวม บทบาท ช่วงเวลา โจทย์ วิธีดำเนินงาน และผลลัพธ์ เว้นหัวข้อที่ยังไม่มีข้อมูลได้ จากนั้นเลือก **เผยแพร่** และกดบันทึก จะมีลิงก์ **เปิดหน้าผลงาน / แชร์ลิงก์** แสดงให้ใช้งาน

เพิ่มผลงานใหม่ด้วย **เพิ่มผลงาน** ระบบสร้างรหัสและลิงก์ใหม่ให้ การเปลี่ยนชื่อโปรเจกต์ไม่เปลี่ยนลิงก์เดิม หากแก้ JSON เอง ให้ใช้ `id` ไม่ซ้ำกันและคงรหัสเดิมเมื่อแก้ชื่อ

หากใช้ GitHub Pages แบบ static ให้ส่งออก JSON ไปอัปเดตตามขั้นตอนด้านบน หน้ารายละเอียดใช้ข้อมูลชุดเดียวกับหน้ารวม ไม่ต้องสร้างไฟล์ HTML แยกด้วยมือทุกโปรเจกต์ ลิงก์ของรหัสที่ไม่มีอยู่หรือฉบับร่างในข้อมูลที่โหลดได้จะแสดง “ไม่พบผลงานนี้”

## เตรียมไฟล์ไว้ขึ้น GitHub ภายหลัง

รันใน PowerShell:

```powershell
./scripts/package.ps1
```

ไฟล์ ZIP จะอยู่ใน `releases/` และแยกเป็น:

- **mypublic-pages-…zip** — เฉพาะหน้าเว็บ static แตกไฟล์ภายในลงโฟลเดอร์ `docs` ของ repository แล้วเลือก Pages → Deploy from a branch → `main` → `/docs`
- **mypublic-source-…zip** — source code ครบทั้งหน้าเว็บ/backend/tests พร้อม GitHub Actions แตกไฟล์ที่ระดับรากของ repository แล้วเลือก Pages → GitHub Actions

ชุดไฟล์ไม่รวม Laravel template เดิม, node_modules, ฐานข้อมูล, รหัสผ่าน, OTP หรือไฟล์ `.env` ในเครื่อง แต่ข้อมูลโปรไฟล์และอีเมลติดต่อที่ใส่ใน `docs/data/portfolio.json` เป็นข้อมูลสาธารณะ

Source code เชื่อมกับ `nyequirel/myportal` แล้ว ส่วนการเข้าสู่ระบบเจ้าของพอร์ตด้วย OTP จริงยังต้องตั้ง SMTP และ hosting backend ตาม [คู่มือฉบับเต็ม](README.md)
